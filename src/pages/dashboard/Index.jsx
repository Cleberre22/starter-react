import React from 'react';
import MenuHeader from "../../components/auth/MenuHeader";

const index = () => {
    return (
        <div>
            <MenuHeader />
            <h1>DASHBOARD</h1>
        </div>
    );
};

export default index;
